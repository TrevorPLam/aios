# ----------------------------------------------------------------------------
# AIOS Environment Configuration (Example)
# ----------------------------------------------------------------------------
# This file documents required and optional environment variables.
# Copy to .env and update values for your environment.
# NEVER commit real secrets.
# Keep this file aligned with current code usage and environment docs.

# ----------------------------------------------------------------------------
# Core Runtime
# ----------------------------------------------------------------------------
# Runtime environment: development | production | test
NODE_ENV=development

# API server port (defaults to 5000 if unset)
PORT=5000

# JWT signing secret for API authentication (required in production)
JWT_SECRET=change-me-in-production

# Server log level: error | warn | info | debug
LOG_LEVEL=info

# ----------------------------------------------------------------------------
# Expo / Client Configuration
# ----------------------------------------------------------------------------
# Public domain used by the Expo client and API base URL resolution.
# Example (local): localhost:5000
EXPO_PUBLIC_DOMAIN=localhost:5000

# Optional explicit API base URL for mobile/web clients (used in docs/examples).
EXPO_PUBLIC_API_URL=http://localhost:5000

# Analytics toggle for client instrumentation (defaults to true when unset)
EXPO_PUBLIC_ANALYTICS_ENABLED=true

# ----------------------------------------------------------------------------
# Replit / Hosted Dev (optional)
# ----------------------------------------------------------------------------
# Replit dev domain used for CORS and Expo hosting.
REPLIT_DEV_DOMAIN=your-domain.repl.co

# Comma-separated list of additional Replit domains to allow in CORS.
REPLIT_DOMAINS=example-1.repl.co,example-2.repl.co

# Internal Replit deployment domain (used for build/hosting scripts).
REPLIT_INTERNAL_APP_DOMAIN=internal.repl.co

# ----------------------------------------------------------------------------
# Database (Drizzle/Postgres)
# ----------------------------------------------------------------------------
# Primary database connection string for migrations and server runtime.
DATABASE_URL=postgresql://postgres:password@localhost:5432/aios_dev

# Optional Postgres component variables (used by infrastructure docs/templates).
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=aios_dev

# ----------------------------------------------------------------------------
# Django Legacy Backend (if enabled)
# ----------------------------------------------------------------------------
# Secret key for Django configuration (legacy backend only).
DJANGO_SECRET_KEY=change-me

# ----------------------------------------------------------------------------
# AWS (optional integrations; placeholders only)
# ----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-bucket-name

# ----------------------------------------------------------------------------
# Stripe (optional payments; placeholders only)
# ----------------------------------------------------------------------------
STRIPE_SECRET_KEY=sk_test_change_me
STRIPE_PUBLISHABLE_KEY=pk_test_change_me
STRIPE_WEBHOOK_SECRET=whsec_change_me
