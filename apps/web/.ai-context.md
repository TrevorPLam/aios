# Web Application

This is the web application for AIOS.

## Framework
- React web application
- Uses design system from `@aios/design-system`
- Uses features from `@aios/features`

## Structure
- Follows the standard app structure
- Imports from shared packages
