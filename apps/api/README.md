# Legacy Server Shell

This directory remains for compatibility. The canonical API shell is `apps/api`,
which mounts logic from `packages/*`.

Do not add new endpoints or business logic here; route new work into packages.
